import styled from 'styled-components'

export const RestaurantCardContainer = styled.div`
display: grid;
gap:20px;

  .CardRestaurants {
    max-width: 345px;
  
  }

  .media {
    height: 140px;
  }
`
